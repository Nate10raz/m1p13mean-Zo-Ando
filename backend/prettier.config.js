module.exports = {
    semi: true,            // ajoute un point-virgule à la fin
    singleQuote: true,     // utilise des simples quotes
    trailingComma: 'all',  // virgule finale pour objets, arrays
    printWidth: 100,       // max 100 caractères par ligne
    tabWidth: 2,           // largeur de tabulation
    arrowParens: 'always'  // parenthèses autour des arguments fléchés
};
